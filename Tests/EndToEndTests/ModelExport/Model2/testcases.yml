dataDir: .
tags:
    # running on every BVT job in 'E' (Examples) leg
    - bvt-e ((flavor=='release') & (device=='gpu')) 
    # running every Nightly job in 'E' (Examples) leg
    - nightly-e

testCases:
  CNTK Run must be completed:
    patterns:
      - ^COMPLETED

